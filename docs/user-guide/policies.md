# Policies

Policy Studio defines ordered First Match Wins rules and a final action. Node Pool and Rule Set
references are validated by the server. Compiler previews use the same Mihomo, sing-box, and Raw
VLESS adapters as public subscriptions.

A policy used by subscriptions cannot be deleted. Resolve its subscriptions first; related
resources are never deleted automatically.
