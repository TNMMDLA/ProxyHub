# Rule Sets

Rule Sets provide reusable manual or remote match data. Remote refresh keeps the Last Known Good
cache when a fetch fails. Readiness treats a usable stale cache as a warning and a missing usable
cache as blocking.

Rule Sets referenced by policy rules cannot be deleted. Open Diagnostics with the Rule Sets tab to
investigate refresh and cache state.
