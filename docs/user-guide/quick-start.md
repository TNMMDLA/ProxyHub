# Quick Start

Dashboard Quick Start is calculated from current resources, not click history. It guides an
administrator through server registration, Reality node creation and validation, node pool and
policy setup, optional Rule Set setup, subscription creation, readiness, and client import.

The checklist never creates or changes resources automatically. A blocked step links to the page
where its prerequisite can be resolved. Setup polling uses a short cache and does not create audit
entries.
